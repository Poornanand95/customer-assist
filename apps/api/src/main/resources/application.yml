server:
  port: 8080

spring:
  application:
    name: assistlink-api

assistlink:
  # Comma-separated list (Spring @Value binding for arrays is awkward in YAML lists)
  cors:
    allowed-origins: "http://localhost:3000,http://127.0.0.1:3000"
